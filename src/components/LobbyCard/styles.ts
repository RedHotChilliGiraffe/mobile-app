import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  text: {
    fontWeight: '600',
    textAlign: 'center',
  },
  background: {
    width: '100%',
    marginVertical: 10,
    borderRadius: 10,
    padding: 5,
    opacity: 0.8,
    alignItems: 'center',
  },
});
