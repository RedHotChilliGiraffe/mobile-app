import {StyleSheet} from 'react-native';

export const unregisterStyles = StyleSheet.create({
  container: {
    flex: 1,
  },
  image: {
    marginTop: 80,
    marginBottom: 40,
    justifyContent: 'center',
    width: 300,
    height: 300,
  },
  buttons: {
    flex: 0.5,
    alignItems: 'center',
    justifyContent: 'center',
    gap: 20,
  },
});
